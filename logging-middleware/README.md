## Logging Middleware

This project uses a custom logging middleware.

Features

- API request logging
- Error logging
- Timestamp logging
- Response logging

Every API request passes through the middleware before execution.
